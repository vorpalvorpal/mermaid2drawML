% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/word_diagram.R
\name{mermaid_to_word}
\alias{mermaid_to_word}
\title{Convert a Mermaid diagram to a Word-embeddable object}
\usage{
mermaid_to_word(mermaid_code, start_id = 1L, timeout = 120L, ...)
}
\arguments{
\item{mermaid_code}{A character string of Mermaid flowchart syntax.}

\item{start_id}{Integer. Starting shape ID. Increment between diagrams in
the same document to avoid ID collisions. Default \code{1L}.}

\item{timeout}{Integer. Seconds to allow for the Node.js render. Default 120.}

\item{...}{Additional arguments passed to \code{\link[=build_diagram_xml]{build_diagram_xml()}}, e.g.
\code{page_width_in}, \code{margin_in}, \code{default_stroke}.}
}
\value{
A \code{mermaid_word_diagram} object with fields:
\itemize{
\item \code{xml}: DrawingML XML string
\item \code{svg_data}: parsed SVG geometry (nodes, edges, viewbox)
\item \code{ast}: mermaid AST from \verb{@mermaid-js/parser}
\item \code{next_id}: next available shape ID for chaining documents
}
}
\description{
Renders the diagram via \verb{@mermaid-js/mermaid-cli} (for layout and SVG) and
\verb{@mermaid-js/parser} (for the AST), then converts the result to DrawingML
XML suitable for insertion into a Word document.
}
\details{
Requires Node.js and the package's Node.js dependencies. Run
\code{\link[=setup_mermaid2drawml]{setup_mermaid2drawml()}} once after installation.
}
\examples{
\dontrun{
d <- mermaid_to_word("flowchart TB\n  A[Start] --> B[End]")
print(d)
}
}
